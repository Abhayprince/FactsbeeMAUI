<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="FactsbeeMAUI.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:FactsbeeMAUI"
    xmlns:pages="clr-namespace:FactsbeeMAUI.Pages"
    Shell.FlyoutBehavior="Flyout">

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />
    
    <ShellContent
        Title="Category Facts"
        ContentTemplate="{DataTemplate pages:CategoryFactsPage}"
        Route="MainPage" />
    
    <ShellContent
        Title="Fact Detail"
        ContentTemplate="{DataTemplate pages:FactDetailsPage}"
        Route="MainPage" />

</Shell>
